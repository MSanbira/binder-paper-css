// JS entry for bundlers. This allows `import 'binder-paper-css'` to include the CSS.
import './style.css';

// Optionally export the path to the CSS for manual linking if needed.
export const cssPath = 'style.css';
export default {};
